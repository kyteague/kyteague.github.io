---
interface Props {
  data: Record<string, unknown>;
}

const serializedData = JSON.stringify(Astro.props.data).replace(
  /</g,
  "\\u003c",
);
---

<script is:inline type="application/ld+json" set:html={serializedData} />
